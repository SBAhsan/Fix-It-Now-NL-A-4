model Category {
    id          String    @id @default(uuid())
    name        String    @unique
    description String?
    isActive    Boolean
    createdAt   DateTime  @default(now())
    services    Service[]

    @@map("categories")
}
